typedef: 常常用于给 复杂的类型命名

typedef char (* FRPTC())[5];

FRPTC(): 函数

* FRPTC(): 此函数 返回值为 指针

(*FRPTC())[5]: FRPTC 是一个函数类型，返回值 是内含5个char类型元素的数组     [函数指针]


eg:
1) char p[5];        //p 是由5个char类型元素构成的数组

2) char (*pt)[5];    //pt 是一个(行)指针[指向数组的指针]，
                       指向由5个char类型元素构成的数组

拓展：指针的指针，指针数组，数组指针

指针数组：数组中都是指针             eg:字符串数组
数组指针：指针，指向数组             2)eg:字符串
指针的指针： 数据类型 ** 变量名；

3) char (*pt())[5];  //pt 是一个函数，函数 返回值是一个 
                       由五个char类型元素构成的数组 的指针
